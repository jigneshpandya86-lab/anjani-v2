{
  "name": "anjani-sms-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for SMS background sending",
  "main": "processSmsJobs.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.0.0"
  },
  "engines": {
    "node": "20"
  }
}
